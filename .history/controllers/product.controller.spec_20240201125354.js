// product.controller.spec.js
const { listProducts, listProductById, uploadProduct, updateProduct, deleteProduct } = require('./product.controller');

describe('Product Controller', () => {
  // Escribe tus pruebas aquí
});